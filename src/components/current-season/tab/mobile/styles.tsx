import styled from "styled-components";
import { Tabs, TabsWrapper } from "../styles";

export const MobileTabsWrapper = styled(TabsWrapper)`
  padding: 0px;
`;

export const MobileTabs = styled(Tabs)`
  padding: 16px;
`;
